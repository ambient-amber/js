/*
    Всего 7 типов данных: null, undefined, boolean, number, string, object, symbol

    False values: '', 0, null, undefined, NaN, false
    if (false) {}

    True values: [], {}, function
    if (true) {}
*/

// '42px' - 2 = NaN
// null + 2 = 2
// undefined + 2 = NaN

// == vs ===
// == - делает приведение к типам
// === - не делает, поэтому 2 != '2'